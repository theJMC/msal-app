<nav class="navbar navbar-expand-lg navbar-dark bg-dark" id="navBar">
    <div class="container">
        <a class="navbar-brand" href="/">
            <i class="bi bi-exclude d-inline-block align-top"></i>
            {{title}}
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav nav-pills nav-fill">
                {{#if back_active}}
                    <div class="ms-auto gap-2 d-block">
                        <a class="btn btn-secondary" href="/"><i class="bi bi-home"></i>  Home</a>
                        <a class="btn btn-secondary" onclick="history.back()"><i class="bi bi-backspace"></i>  Back</a>
                    </div>

                {{/if}}
                {{#each navs}}
                    {{#if active}}
                    <li class="nav-item">
                        <a class="nav-link active" href="#">{{name}}<span class="sr-only">(current)</span></a>
                    </li>  
                    {{else}}
                    <li class="nav-item">
                        <a class="nav-link" href="{{link}}">{{name}}</a>
                    </li> 
                    {{/if}}
                {{/each}}
            </ul>
            <div class="ms-auto d-grid gap-2 d-md-block">
                {{#if loggedIn}}
                    <a type="button" class="btn btn-outline-secondary" href="/profile" aria-expanded="false"><div class="username"></div></a>
                    <a type="button" class="btn btn-outline-danger" href="/logout" aria-expanded="false">Log Out</a>
                {{else}}
                    <a type="button" class="btn btn-outline-success" href="/login" aria-expanded="false">Log In</a>
                {{/if}}
            </div>
        </div>
    </div>
  </nav>